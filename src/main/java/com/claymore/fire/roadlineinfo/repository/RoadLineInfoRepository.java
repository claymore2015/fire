package com.claymore.fire.roadlineinfo.repository;

import com.claymore.fire.roadlineinfo.entity.RoadLineInfoEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RoadLineInfoRepository extends JpaRepository<RoadLineInfoEntity, Integer>{
}
